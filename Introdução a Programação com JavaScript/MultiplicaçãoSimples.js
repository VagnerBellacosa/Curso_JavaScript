// Multiplicacao Simples

// captura dois valores
let valor1 = parseInt(gets());
let valor2 = parseInt(gets());

// multiplica  valores
let total = valor1 * valor2; // Altere o valor da variável com o cálculo esperado 

// lista valores
console.log("PROD = " + total);