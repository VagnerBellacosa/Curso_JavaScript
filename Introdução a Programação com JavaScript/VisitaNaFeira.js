// Visita Na Feira

// obtem texto externo
let line = gets().split(" ");

// tranforma string em array e naveganda nas ocorrencias
// convertendo texo em inteiro 
let A = parseInt(line[0]);
let B = parseInt(line[1]);

// efetua calculo
let total = A + B // Altere o valor da variável com o cálculo esperado

// lista na consola
console.log("X = " + total);