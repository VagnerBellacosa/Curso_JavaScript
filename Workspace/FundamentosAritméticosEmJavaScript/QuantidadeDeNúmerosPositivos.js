// Quantidade de Números Positivos

// declaraçao de variaveis
var Ct_Total = 0
var Fl_Stop = true;

// continua enquanto os valores recibido sao valaidos
while (Fl_Stop) {

    // obtem novo numero
    let valor1 = parseInt(gets(), 0);

    // verifica se é um numero valido 
    if (valor1 == null || (isNaN(valor1)))
        Fl_Stop = false;
} else {
    el /
        if (valor1 > 0) {
            Ctotal++;
        }
}
mn§h
// exibe o numero na consola
console.log(Ct_Total + " valores positivos");