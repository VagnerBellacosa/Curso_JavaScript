// Slice
// Se negativo, é o mesmo que executar str.length + endIndex onde str.length é o tamanho da string
// (por exemplo, se endIndex é -3, é como executar str.length - 3).

const nome = 'Vagner Renato';

console.log(nome.slice(0, -3)); // retorna

// Vagner Ren