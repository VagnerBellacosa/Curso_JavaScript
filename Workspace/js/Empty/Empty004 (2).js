// Outro Exemplo: Uma declaração if...else declaração sem chaves ({ }). Se três for verdadeiro,
// nada acontecerá, quatro não importa e também a função launchRocket ( ) no caso contrário não 
// será executada.

if (one)
    doOne();
else if (two)
    doTwo();
else if (three)
; // nothing here
else if (four)
    doFour();
else
    launchRocket();