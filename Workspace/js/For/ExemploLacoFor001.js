// Usando for
// A declaração for começa declarando a variável i e inicializando-a como 0. 
// Ela verifica se i é menor que nove, executa as duas instruções subsequentes
// e incrementa 1 a variável i após cada passagem pelo loop.

for (var i = 0; i < 9; i++) {
   console.log(i);
   // more statements
}
